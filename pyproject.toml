[project]
name = "image-compress"
version = "0.1.0"
description = "Recursively compress JPEG/PNG/WebP images without changing dimensions"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "Pillow>=10.3.0",
]

[dependency-groups]
dev = [
    "pytest>=7.0.0",
]
